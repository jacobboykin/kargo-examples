# git-tag + git-push tags (v1.10)

Demonstrates the new `git-tag` step and `git-push` tag pushing.

Creates an annotated tag from the promoted commit, then pushes it to the remote.
This simulates tagging a verified build after it passes through a testing Stage.
